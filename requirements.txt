# Frutiger Hardware Monitor - Python Dependencies

# Core Web Framework
Flask==3.0.0
flask-cors==4.0.0

# System Monitoring
psutil==5.9.6

# HTTP Requests (dla LibreHardwareMonitor API)
requests==2.31.0

# Dodatkowe zależności dla Flask
Werkzeug==3.0.1
click==8.1.7
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.3

# Opcjonalne (tylko na Windows dla WMI jako fallback)
# Odkomentuj jeśli chcesz spróbować również WMI (nie jest wymagane)
# pywin32==306
# WMI==1.5.1
